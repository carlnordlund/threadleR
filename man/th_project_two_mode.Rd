% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/threadle_client.R
\name{th_project_two_mode}
\alias{th_project_two_mode}
\title{Project a two-mode layer to a one-mode layer}
\usage{
th_project_two_mode(
  network,
  layername,
  method = c("count", "newman", "binary"),
  newlayername = NULL
)
}
\arguments{
\item{network}{A \code{threadle_network} object or a character string giving
the name of a network in the Threadle CLI environment.}

\item{layername}{Name of the two-mode layer to project.}

\item{method}{Projection method: \code{"count"} (default), \code{"newman"}, or \code{"binary"}.}

\item{newlayername}{Optional name for the resulting one-mode layer. If \code{NULL},
a default name is used by the backend.}
}
\value{
\code{NULL}, invisibly.
}
\description{
\code{th_project_two_mode()} creates a one-mode projection of a two-mode (bipartite /
hyperedge) layer, adding the resulting layer to the same network.
}
\details{
Three projection methods are available:
\describe{
\item{\code{"count"}}{Edge weight equals the number of shared hyperedges (default).}
\item{\code{"binary"}}{Edge exists if nodes share at least one hyperedge; weight is 1.}
\item{\code{"newman"}}{Newman (2001) weighted projection; weight for each shared
hyperedge of size \eqn{k} contributes \eqn{1/(k-1)}.}
}
}
\examples{
\dontshow{if (th_is_available()) withAutoprint(\{ # examplesIf}
th_start_threadle()

ns <- th_create_nodeset("ns", createnodes = 4)
net <- th_create_network("net", ns)
th_add_layer(net, "t2", mode = 2)
th_add_hyper(net, "t2", "club1", nodes = c(1, 2, 3))
th_add_hyper(net, "t2", "club2", nodes = c(2, 3, 4))
th_project_two_mode(net, "t2", method = "count", newlayername = "proj")
th_get_edge(net, "proj", node1id = 2, node2id = 3)
th_stop_threadle()
\dontshow{\}) # examplesIf}
}
