% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/threadle_client.R
\name{th_cmd}
\alias{th_cmd}
\title{Send a raw command to the Threadle backend}
\usage{
th_cmd(cmd, args = list(), assign = NULL, type = NULL)
}
\arguments{
\item{cmd}{Command name string (e.g. \code{"density"}, \code{"i"}).}

\item{args}{Named list of arguments. \verb{threadle_*} objects are automatically
resolved to their backend name.}

\item{assign}{Optional variable name to assign the result to in the backend.}

\item{type}{If \code{assign} is set, the type string for the returned handle
(e.g. \code{"network"}, \code{"nodeset"}). If \code{NULL}, the payload is returned directly.}
}
\value{
A \verb{threadle_*} handle if \code{assign} and \code{type} are both set; otherwise
the payload value, invisibly.
}
\description{
\code{th_cmd()} is a low-level escape hatch that sends any command directly to
the Threadle CLI, bypassing the typed wrapper functions. Useful for commands
not yet wrapped in threadleR, or for advanced scripting.
}
