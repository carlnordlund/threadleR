% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/threadle_client.R
\name{th_rwfpt}
\alias{th_rwfpt}
\title{Random walker mean first passage time distances}
\usage{
th_rwfpt(
  name,
  network,
  attrname,
  maxsteps,
  layernames = NULL,
  walkfactor = 1,
  minpairobs = 10L,
  balanced = FALSE,
  weighted = FALSE
)
}
\arguments{
\item{name}{Name of the variable to assign the result network to.}

\item{network}{A \code{threadle_network} object or a character string giving
the name of a network in the Threadle CLI environment.}

\item{attrname}{Name of the node attribute defining category membership.}

\item{maxsteps}{Maximum number of steps per walk.}

\item{layernames}{Optional character vector of layer names to walk on.
If \code{NULL}, all layers are used.}

\item{walkfactor}{Multiplier controlling the number of walks relative to
network size. Defaults to \code{1.0}.}

\item{minpairobs}{Minimum number of observations required per category pair
to report a result. Defaults to \code{10}.}

\item{balanced}{Logical; if \code{TRUE}, balances walk starts across categories.
Defaults to \code{FALSE}.}

\item{weighted}{Logical; if \code{TRUE}, uses edge weights to bias walk steps.
Defaults to \code{FALSE}.}

\item{backtrack}{Logical; if \code{TRUE}, allows walkers to return to the
previous node. Defaults to \code{FALSE}.}

\item{savesteps}{Logical; if \code{TRUE}, saves per-step data. Defaults to \code{FALSE}.}
}
\value{
A \code{threadle_network} object containing result layers.
}
\description{
\code{th_rwfpt()} estimates inter-categorical distances as mean first passage
times (MFPT) using random walkers, based on a node attribute defining
category membership. Returns a network containing result layers with MFPT
estimates, standard deviations, observation counts, and coverage.
}
\examples{
\dontshow{if (th_is_available()) withAutoprint(\{ # examplesIf}
th_start_threadle()

ns <- th_create_nodeset("ns", createnodes = 6)
net <- th_create_network("net", ns)
th_add_layer(net, "l1", mode = 1, directed = FALSE, valuetype = "binary")
th_add_edge(net, "l1", node1id = 1, node2id = 2)
th_add_edge(net, "l1", node1id = 2, node2id = 3)
th_add_edge(net, "l1", node1id = 3, node2id = 4)
th_add_edge(net, "l1", node1id = 4, node2id = 5)
th_define_attr(net, "grp", "int")
th_set_attr(net, nodeid = 1, attrname = "grp", attrvalue = 1)
th_set_attr(net, nodeid = 2, attrname = "grp", attrvalue = 1)
th_set_attr(net, nodeid = 3, attrname = "grp", attrvalue = 2)
th_set_attr(net, nodeid = 4, attrname = "grp", attrvalue = 2)
th_set_attr(net, nodeid = 5, attrname = "grp", attrvalue = 1)
net <- th_rwfpt("net", net, attrname = "grp", maxsteps = 100L, minpairobs = 5L)
th_stop_threadle()
\dontshow{\}) # examplesIf}
}
