% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/threadle_client.R
\name{th_get_random_edge}
\alias{th_get_random_edge}
\title{Get a random edge from a layer}
\usage{
th_get_random_edge(network, layername, maxattempts = 100)
}
\arguments{
\item{network}{A \code{threadle_network} object or a character string giving the name of
a network in the Threadle CLI environment.}

\item{layername}{Name of the layer to sample from (one-mode or two-mode).}

\item{maxattempts}{Maximum number of uniform random node-pair attempts before switching to
the fallback heuristic (two-mode layers only). Defaults to 100.}
}
\value{
A named list with:
\describe{
\item{node1}{Integer scalar; ID of the first node.}
\item{node2}{Integer scalar; ID of the second node.}
\item{value}{Numeric scalar; edge value between \code{node1} and \code{node2}.}
}
}
\description{
\code{th_get_random_edge()} gets a randomly sampled edge from a specified layer.
}
\examples{
\dontshow{if (th_is_available()) withAutoprint(\{ # examplesIf}
th_start_threadle()

ns <- th_create_nodeset("ns", createnodes = 5)
net <- th_create_network("net", ns)
th_add_layer(net, "l1", mode = 1)
th_generate(net, "l1", type = "er", p = 1)
th_get_random_edge(net, "l1")
th_stop_threadle()
\dontshow{\}) # examplesIf}
}
