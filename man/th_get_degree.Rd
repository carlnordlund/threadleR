% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/threadle_client.R
\name{th_get_degree}
\alias{th_get_degree}
\title{Get the degree of a specific node}
\usage{
th_get_degree(
  network,
  nodeid,
  layernames = NULL,
  direction = "out",
  unique = TRUE
)
}
\arguments{
\item{network}{A \code{threadle_network} object or a character string giving
the name of a network in the Threadle CLI environment.}

\item{nodeid}{Node ID.}

\item{layernames}{Optional character vector of layer names to restrict the
count to. If \code{NULL}, all layers are used.}

\item{direction}{Which ties to count: \code{"both"}, \code{"in"}, or \code{"out"} (default).}

\item{unique}{Logical; if \code{TRUE}, deduplicate alters across layers. Defaults
to \code{TRUE}.}
}
\value{
An integer scalar giving the degree of the node.
}
\description{
\code{th_get_degree()} returns the degree of a single node, optionally restricted
to specific layers and a specific tie direction.
}
\details{
Unlike \code{th_degree()}, which computes degree centrality for all nodes and
stores the result as a node attribute, \code{th_get_degree()} retrieves the degree
of one specific node and returns it directly.
}
\examples{
\dontshow{if (th_is_available()) withAutoprint(\{ # examplesIf}
th_start_threadle()

ns <- th_create_nodeset("ns", createnodes = 4)
net <- th_create_network("net", ns)
th_add_layer(net, "l1", mode = 1, directed = FALSE, valuetype = "binary")
th_add_edge(net, "l1", node1id = 1, node2id = 2)
th_add_edge(net, "l1", node1id = 1, node2id = 3)
th_get_degree(net, nodeid = 1, layernames = "l1", direction = "both")
th_stop_threadle()
\dontshow{\}) # examplesIf}
}
